/**
 * Совпадает с `--palette-accent-primary` в `styles/tokens/palette.css`.
 */
export const PALETTE_PRIMARY = '#ffb829' as const

/** Акцент в тёмной теме (`html[data-theme='dark']` в palette.css). */
export const PALETTE_PRIMARY_DARK = '#f0b24a' as const

export const CHART_GRID_STROKE = '#cbd5e1' as const
